---
title: "PEC_1"
output: html_document
date: "2025-03-25"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```



```{r load_data}
library(readxl)
path<-"ST000291noNAs.XLSX"
sheetnames <- excel_sheets(path)
mylist <- lapply(excel_sheets(path), read_excel, path = path)
names(mylist) <- sheetnames
list2env(mylist ,.GlobalEnv)
```



```{r create_class, echo=FALSE}
if (!require(BiocManager)){
  install("BiocManager")
}

char_cols_feat <- unlist(lapply(features, is.character))
numfeatures<-as.data.frame(sapply(features[,char_cols_feat],as.numeric))
numfeatures<-numfeatures[order(numfeatures$PubChemID),]

rownames<-names(data)
colnames<-numfeatures$PubChemID

data<-numfeatures[,-1]

data2<-as.data.frame(t(data))

#rownames(features2)<-features2[,1]
char_cols <- unlist(lapply(data, is.character))
numdata<-as.data.frame(sapply(data[,char_cols],as.numeric))

```

